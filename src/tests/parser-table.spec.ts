import { ParserTable } from 'framework/compiler/parser-table';

describe('Parser table', () => {
	let table:ParserTable;
	beforeEach(() => {
		
	});

	it('should add to 2',()=>{//sample
		expect(1+1).toEqual(2);
	});
});